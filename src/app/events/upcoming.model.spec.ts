/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Upcoming} from './upcoming.model';

describe('Upcoming', () => {
  it('should create an instance', () => {
    expect(new Upcoming()).toBeTruthy();
  });
});
